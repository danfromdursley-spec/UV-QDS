# Field Kit
A small, portable set of reproducibility-first tools:

- H0 variance micro-fit quick check
- Galaxy rotation RMS micro-check
- Session metadata + JSON export
- Notes + device/perf logging

The point:
tiny tests that show discipline + credibility.
